function plot_apogee(h_wo,h_AA,t_wo,t_AA,ft);

if(ft)
    ft2m = 0.3048; %feet to meters
    h_wo = h_wo.*(1/ft2m);
    h_AA = h_AA.*(1/ft2m);
    str = "height h in feet";
else
    
    str = "height h in met";
end
figure
plot(t_wo,h_wo)
hold on
plot(t_AA,h_AA)
grid on
xlabel(